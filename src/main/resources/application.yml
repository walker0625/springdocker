spring:
  datasource:
    url: jdbc:mysql://db-server:3306/mydb # compose.yml? service ??? dns ??
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: cache-server # compose.yml? service ??
      port: 6379

